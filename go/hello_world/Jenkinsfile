podTemplate(
  cloud: 'kubernetes',
  namespace: 'jenkins',
  activeDeadlineSeconds: '100',
  showRawYaml: 'false',
  containers: [
  containerTemplate(
    name: 'golang',
    image: 'golang:latest',
    ttyEnabled: true,
    command: 'cat'
  )
]) {
  node(POD_LABEL) {
  // project variables
  def project_directory = "${WORKSPACE}/go/hello_world"

    try {
      container('golang') {

        stage("Get Golang Project") {
          git url: 'https://github.com/Condoamanti/programming.git'
        }

        stage('Build Golang Project') {
          sh """
          cd ${project_directory};
          go run ./hello_world.go;
          go build ./hello_world.go;
          ./hello_world;
          """
        }
      } // container end
    } catch (err) {
      echo "Caught: ${err}"
      currentBuild.result = 'FAILURE'
    } finally {
    }
  }
}
